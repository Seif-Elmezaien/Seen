<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".ui.authentication.fragments.AccountMadeFragment">

    <!-- ==================== Guideline ==================== -->

    <androidx.constraintlayout.widget.Guideline
        android:id="@+id/guideline_h40"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        app:layout_constraintGuide_percent="0.4" />

    <!-- ==================== Celebration Image ==================== -->

    <ImageView
        android:id="@+id/iv_celebration"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginStart="20dp"
        android:src="@drawable/celebration"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintBottom_toTopOf="@+id/guideline_h40" />

    <!-- ==================== Text ==================== -->

    <TextView
        android:id="@+id/tvAccountMade"
        android:layout_width="340dp"
        android:layout_height="wrap_content"
        android:layout_marginTop="30dp"
        android:text="@string/account_made"
        android:textColor="@color/text_questionnaire"
        android:textSize="34sp"
        android:fontFamily="@font/cairo_bold"
        android:gravity="center"
        app:layout_constraintTop_toBottomOf="@+id/iv_celebration"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent" />

    <TextView
        android:id="@+id/tvWelcomeMsg"
        android:layout_width="360dp"
        android:layout_height="wrap_content"
        android:layout_marginTop="20dp"
        android:text="@string/welcome_msg"
        android:textColor="@color/text_questionnaire"
        android:textSize="20sp"
        android:fontFamily="@font/cairo"
        android:gravity="center"
        app:layout_constraintTop_toBottomOf="@+id/tvAccountMade"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent" />

    <!-- ==================== Login Button ==================== -->

    <com.google.android.material.button.MaterialButton
        android:id="@+id/btnLogin"
        android:layout_width="200dp"
        android:layout_height="65dp"
        android:layout_marginTop="30dp"
        android:text="@string/login"
        android:textColor="@color/white"
        android:textSize="18sp"
        android:fontFamily="@font/cairo_bold"
        android:elevation="4dp"
        app:cornerRadius="8dp"
        app:layout_constraintTop_toBottomOf="@+id/tvWelcomeMsg"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent" />

</androidx.constraintlayout.widget.ConstraintLayout>